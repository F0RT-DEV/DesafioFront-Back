<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ClimaTrack - Painel Climático</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        :root {
            --primary: #4361ee;
            --secondary: #3f37c9;
            --accent: #4895ef;
            --light: #f8f9fa;
            --dark: #212529;
            --success: #4cc9f0;
            --warning: #f72585;
            --gray: #6c757d;
            --bg: #f0f2f5;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            background-color: var(--bg);
            color: var(--dark);
            transition: all 0.3s ease;
        }

        .container {
            display: flex;
            min-height: 100vh;
        }

        /* Sidebar */
        .sidebar {
            width: 250px;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: white;
            padding: 20px 0;
            transition: all 0.3s ease;
            box-shadow: 2px 0 10px rgba(0, 0, 0, 0.1);
            position: relative;
            z-index: 10;
        }

        .sidebar-header {
            padding: 0 20px 20px;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            margin-bottom: 20px;
        }

        .sidebar-header h2 {
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 1.5rem;
        }

        .sidebar-header i {
            font-size: 1.8rem;
            color: var(--success);
        }

        .nav-menu {
            list-style: none;
        }

        .nav-item {
            margin: 5px 0;
        }

        .nav-link {
            display: flex;
            align-items: center;
            padding: 12px 20px;
            color: white;
            text-decoration: none;
            transition: all 0.3s ease;
            border-left: 3px solid transparent;
        }

        .nav-link:hover, .nav-link.active {
            background-color: rgba(255, 255, 255, 0.1);
            border-left: 3px solid var(--accent);
        }

        .nav-link i {
            margin-right: 10px;
            font-size: 1.1rem;
        }

        /* Main Content */
        .main-content {
            flex: 1;
            padding: 20px;
            transition: all 0.3s ease;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 1px solid #e0e0e0;
        }

        .header h1 {
            font-size: 1.8rem;
            color: var(--dark);
        }

        /* Dashboard */
        .dashboard {
            display: grid;
            grid-template-columns: 1fr;
            gap: 20px;
            margin-bottom: 30px;
        }

        .main-card {
            background: white;
            border-radius: 10px;
            padding: 30px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
            transition: all 0.3s ease;
            animation: fadeIn 0.5s ease;
            text-align: center;
        }

        .main-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
        }

        .main-title {
            font-size: 1.8rem;
            color: var(--dark);
            margin-bottom: 20px;
            line-height: 1.6;
        }

        .temperature-icon {
            font-size: 5rem;
            color: var(--primary);
            margin: 20px 0;
        }

        .temperature-display {
            font-size: 3.5rem;
            font-weight: 600;
            color: var(--primary);
            display: flex;
            align-items: flex-start;
            justify-content: center;
        }

        .temperature-display span {
            font-size: 2rem;
            margin-top: 5px;
        }

        .current-time {
            font-size: 1.2rem;
            color: var(--gray);
            margin-top: 15px;
        }

        /* Chart */
        .chart-container {
            background: white;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
            margin-bottom: 30px;
            animation: slideUp 0.5s ease;
        }

        .chart-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .date-filter {
            display: flex;
            gap: 10px;
        }

        .date-input {
            padding: 8px 12px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 0.9rem;
        }

        .btn {
            padding: 8px 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-weight: 500;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }

        .btn-primary {
            background-color: var(--primary);
            color: white;
        }

        .btn-primary:hover {
            background-color: var(--secondary);
        }

        .btn-success {
            background-color: var(--success);
            color: white;
        }

        .btn-success:hover {
            background-color: #3aa8d8;
        }

        /* Modal */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            z-index: 100;
            justify-content: center;
            align-items: center;
            animation: fadeIn 0.3s ease;
        }

        .modal-content {
            background: white;
            border-radius: 10px;
            width: 100%;
            max-width: 500px;
            padding: 25px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
            transform: scale(0.9);
            animation: modalOpen 0.3s ease forwards;
        }

        .modal-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .modal-title {
            font-size: 1.5rem;
            color: var(--dark);
        }

        .close-btn {
            background: none;
            border: none;
            font-size: 1.5rem;
            cursor: pointer;
            color: var(--gray);
        }

        .form-group {
            margin-bottom: 15px;
        }

        .form-label {
            display: block;
            margin-bottom: 5px;
            font-weight: 500;
            color: var(--dark);
        }

        .form-control {
            width: 100%;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 1rem;
            transition: all 0.3s ease;
        }

        .form-control:focus {
            border-color: var(--primary);
            outline: none;
            box-shadow: 0 0 0 3px rgba(67, 97, 238, 0.2);
        }

        /* Registros */
        .records-container {
            display: none;
            animation: fadeIn 0.5s ease;
        }

        .records-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .location-select {
            padding: 8px 12px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 0.9rem;
            min-width: 200px;
        }

        .records-list {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 20px;
        }

        .record-card {
            background: white;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .record-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 5px;
            height: 100%;
            background: var(--primary);
        }

        .record-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
        }

        .record-header {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
        }

        .record-title {
            font-size: 1.2rem;
            color: var(--dark);
        }

        .record-temperature {
            font-size: 1.5rem;
            font-weight: 600;
            color: var(--primary);
        }

        .record-details {
            margin-top: 15px;
        }

        .detail-item {
            display: flex;
            margin-bottom: 8px;
        }

        .detail-label {
            width: 100px;
            color: var(--gray);
        }

        .detail-value {
            flex: 1;
            font-weight: 500;
        }

        /* Animations */
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        @keyframes slideUp {
            from { 
                opacity: 0;
                transform: translateY(20px);
            }
            to { 
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes modalOpen {
            to {
                transform: scale(1);
            }
        }

        /* Responsive */
        @media (max-width: 768px) {
            .container {
                flex-direction: column;
            }
            
            .sidebar {
                width: 100%;
                height: auto;
                padding: 10px;
            }
            
            .nav-menu {
                display: flex;
                overflow-x: auto;
            }
            
            .nav-item {
                margin: 0 5px;
            }
            
            .nav-link {
                padding: 10px 15px;
                border-left: none;
                border-bottom: 3px solid transparent;
            }
            
            .nav-link:hover, .nav-link.active {
                border-left: none;
                border-bottom: 3px solid var(--accent);
            }
            
            .dashboard {
                grid-template-columns: 1fr;
            }
            
            .date-filter {
                flex-direction: column;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Sidebar -->
        <aside class="sidebar">
            <div class="sidebar-header">
                <h2><i class="fas fa-cloud-sun"></i> ClimaTrack</h2>
            </div>
            <ul class="nav-menu">
                <li class="nav-item">
                    <a href="#" class="nav-link active" id="dashboard-link">
                        <i class="fas fa-home"></i> Painel
                    </a>
                </li>
                <li class="nav-item">
                    <a href="#" class="nav-link" id="records-link">
                        <i class="fas fa-list"></i> Registros
                    </a>
                </li>
            </ul>
        </aside>

        <!-- Main Content -->
        <main class="main-content">
            <!-- Dashboard Section -->
            <section id="dashboard-section">
                <div class="header">
                    <h1>Painel Climático</h1>
                    <button class="btn btn-success" id="add-location-btn">
                        <i class="fas fa-plus"></i> Adicionar Local
                    </button>
                </div>

                <div class="dashboard">
                    <div class="main-card">
                        <h2 class="main-title">
                            Bom dia<br>
                            São Paulo, Brasil<br>
                            Sexta-feira, 25 de abril de 2025
                        </h2>
                        <div class="temperature-icon">
                            <i class="fas fa-sun"></i>
                        </div>
                        <div class="temperature-display">
                            24<span>°C</span>
                        </div>
                        <div class="current-time">
                            <i class="far fa-clock"></i> 10:30 AM
                        </div>
                    </div>
                </div>

                <div class="chart-container">
                    <div class="chart-header">
                        <h2>Temperatura nas Últimas Horas</h2>
                        <div class="date-filter">
                            <input type="date" class="date-input" id="start-date">
                            <input type="date" class="date-input" id="end-date">
                            <button class="btn btn-primary" id="filter-btn">
                                <i class="fas fa-filter"></i> Filtrar
                            </button>
                        </div>
                    </div>
                    <canvas id="temperatureChart"></canvas>
                </div>
            </section>

            <!-- Records Section -->
            <section id="records-section" class="records-container">
                <div class="header">
                    <h1>Registros</h1>
                    <button class="btn btn-success" id="add-location-btn-2">
                        <i class="fas fa-plus"></i> Adicionar Local
                    </button>
                </div>

                <div class="records-header">
                    <h2>Locais Registrados</h2>
                    <select class="location-select" id="location-filter">
                        <option value="all">Todos os locais</option>
                        <option value="sp">São Paulo</option>
                        <option value="rj">Rio de Janeiro</option>
                        <option value="bh">Belo Horizonte</option>
                    </select>
                </div>

                <div class="records-list">
                    <div class="record-card">
                        <div class="record-header">
                            <h3 class="record-title">São Paulo</h3>
                            <div class="record-temperature">24°C</div>
                        </div>
                        <div class="record-details">
                            <div class="detail-item">
                                <span class="detail-label">Estado:</span>
                                <span class="detail-value">SP</span>
                            </div>
                            <div class="detail-item">
                                <span class="detail-label">Data:</span>
                                <span class="detail-value">25/04/2025</span>
                            </div>
                            <div class="detail-item">
                                <span class="detail-label">Horário:</span>
                                <span class="detail-value">10:30</span>
                            </div>
                        </div>
                    </div>

                    <div class="record-card">
                        <div class="record-header">
                            <h3 class="record-title">Rio de Janeiro</h3>
                            <div class="record-temperature">28°C</div>
                        </div>
                        <div class="record-details">
                            <div class="detail-item">
                                <span class="detail-label">Estado:</span>
                                <span class="detail-value">RJ</span>
                            </div>
                            <div class="detail-item">
                                <span class="detail-label">Data:</span>
                                <span class="detail-value">24/04/2025</span>
                            </div>
                            <div class="detail-item">
                                <span class="detail-label">Horário:</span>
                                <span class="detail-value">15:45</span>
                            </div>
                        </div>
                    </div>

                    <div class="record-card">
                        <div class="record-header">
                            <h3 class="record-title">Belo Horizonte</h3>
                            <div class="record-temperature">22°C</div>
                        </div>
                        <div class="record-details">
                            <div class="detail-item">
                                <span class="detail-label">Estado:</span>
                                <span class="detail-value">MG</span>
                            </div>
                            <div class="detail-item">
                                <span class="detail-label">Data:</span>
                                <span class="detail-value">23/04/2025</span>
                            </div>
                            <div class="detail-item">
                                <span class="detail-label">Horário:</span>
                                <span class="detail-value">12:15</span>
                            </div>
                        </div>
                    </div>
                </div>
            </section>
        </main>
    </div>

    <!-- Add Location Modal -->
    <div class="modal" id="location-modal">
        <div class="modal-content">
            <div class="modal-header">
                <h3 class="modal-title">Adicionar Local</h3>
                <button class="close-btn" id="close-modal">&times;</button>
            </div>
            <form id="location-form">
                <div class="form-group">
                    <label for="location-name" class="form-label">Nome do Local</label>
                    <input type="text" id="location-name" class="form-control" required>
                </div>
                <div class="form-group">
                    <label for="location-state" class="form-label">Estado (Opcional)</label>
                    <input type="text" id="location-state" class="form-control">
                </div>
                <div class="form-group">
                    <label for="location-date" class="form-label">Data</label>
                    <input type="date" id="location-date" class="form-control" required>
                </div>
                <div class="form-group">
                    <label for="location-time" class="form-label">Horário</label>
                    <input type="time" id="location-time" class="form-control" required>
                </div>
                <div class="form-group">
                    <label for="location-temperature" class="form-label">Temperatura (°C)</label>
                    <input type="number" id="location-temperature" class="form-control" step="0.1" required>
                </div>
                <button type="submit" class="btn btn-primary">
                    <i class="fas fa-save"></i> Salvar
                </button>
            </form>
        </div>
    </div>

    <script>
        // Navigation between sections
        document.getElementById('dashboard-link').addEventListener('click', function(e) {
            e.preventDefault();
            document.getElementById('dashboard-section').style.display = 'block';
            document.getElementById('records-section').style.display = 'none';
            this.classList.add('active');
            document.getElementById('records-link').classList.remove('active');
        });

        document.getElementById('records-link').addEventListener('click', function(e) {
            e.preventDefault();
            document.getElementById('dashboard-section').style.display = 'none';
            document.getElementById('records-section').style.display = 'block';
            this.classList.add('active');
            document.getElementById('dashboard-link').classList.remove('active');
        });

        // Modal functionality
        const modal = document.getElementById('location-modal');
        const addBtn = document.getElementById('add-location-btn');
        const addBtn2 = document.getElementById('add-location-btn-2');
        const closeBtn = document.getElementById('close-modal');

        addBtn.addEventListener('click', function() {
            modal.style.display = 'flex';
        });

        addBtn2.addEventListener('click', function() {
            modal.style.display = 'flex';
        });

        closeBtn.addEventListener('click', function() {
            modal.style.display = 'none';
        });

        window.addEventListener('click', function(e) {
            if (e.target === modal) {
                modal.style.display = 'none';
            }
        });

        // Temperature Chart
        const ctx = document.getElementById('temperatureChart').getContext('2d');
        const temperatureChart = new Chart(ctx, {
            type: 'line',
            data: {
                labels: ['00:00', '03:00', '06:00', '09:00', '12:00', '15:00', '18:00', '21:00'],
                datasets: [{
                    label: 'Temperatura (°C)',
                    data: [18, 17, 18, 22, 25, 27, 24, 21],
                    backgroundColor: 'rgba(67, 97, 238, 0.2)',
                    borderColor: 'rgba(67, 97, 238, 1)',
                    borderWidth: 2,
                    tension: 0.4,
                    fill: true
                }]
            },
            options: {
                responsive: true,
                plugins: {
                    legend: {
                        position: 'top',
                    },
                    tooltip: {
                        mode: 'index',
                        intersect: false,
                    }
                },
                scales: {
                    y: {
                        beginAtZero: false,
                        suggestedMin: 15,
                        suggestedMax: 30
                    }
                }
            }
        });

        // Form submission
        document.getElementById('location-form').addEventListener('submit', function(e) {
            e.preventDefault();
            
            // Here you would typically send the data to your backend
            const name = document.getElementById('location-name').value;
            const state = document.getElementById('location-state').value;
            const date = document.getElementById('location-date').value;
            const time = document.getElementById('location-time').value;
            const temp = document.getElementById('location-temperature').value;
            
            console.log('Novo local adicionado:', { name, state, date, time, temp });
            
            // Close modal and reset form
            modal.style.display = 'none';
            this.reset();
            
            // Show success message (you could implement a toast notification)
            alert('Local adicionado com sucesso!');
        });

        // Filter functionality
        document.getElementById('filter-btn').addEventListener('click', function() {
            const startDate = document.getElementById('start-date').value;
            const endDate = document.getElementById('end-date').value;
            
            // Here you would typically fetch filtered data from your backend
            console.log('Filtrar por:', startDate, 'até', endDate);
            
            // For demo purposes, we'll just show an alert
            if (startDate && endDate) {
                alert(`Filtrando dados de ${startDate} até ${endDate}`);
            } else {
                alert('Por favor, selecione ambas as datas para filtrar');
            }
        });

        // Location filter
        document.getElementById('location-filter').addEventListener('change', function() {
            const selectedLocation = this.value;
            
            // Here you would typically filter the records based on the selection
            console.log('Filtrar locais por:', selectedLocation);
            
            // For demo purposes, we'll just show an alert
            if (selectedLocation !== 'all') {
                alert(`Mostrando apenas registros de ${selectedLocation}`);
            }
        });

        // Update time every minute
        function updateTime() {
            const now = new Date();
            const timeElement = document.querySelector('.current-time');
            if (timeElement) {
                const hours = now.getHours().toString().padStart(2, '0');
                const minutes = now.getMinutes().toString().padStart(2, '0');
                timeElement.innerHTML = `<i class="far fa-clock"></i> ${hours}:${minutes}`;
            }
        }

        setInterval(updateTime, 60000);
        updateTime(); // Initialize immediately
    </script>
</body>
</html>